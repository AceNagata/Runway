# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Editor and OS noise
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Local tooling state. .claude/launch.json is committed — it is how the dev server starts.
.claude/settings.local.json

# Firebase — the local emulator and deploy cache, not the config
.firebase/
firebase-debug.log
.firebaserc.local
tsconfig.tsbuildinfo
